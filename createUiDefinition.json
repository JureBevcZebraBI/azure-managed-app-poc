{
  "$schema": "https://schema.management.azure.com/schemas/0.1.2-preview/CreateUIDefinition.MultiVm.json#",
  "handler": "Microsoft.Azure.CreateUIDef",
  "version": "0.1.2-preview",
  "parameters": {
    "config": {
      "basics": {}
    },
    "basics": [
      {
        "name": "adminPassword",
        "type": "Microsoft.Common.TextBox",
        "label": "Administrator password",
        "toolTip": "Enter a strong password (min 12 chars, upper, lower, number, special).",
        "constraints": {
          "required": true,
          "regex": "^(?=.*[a-z])(?=.*[A-Z])(?=.*\\d)(?=.*[!@#$%^&*()_+\\-={}\\[\\]:;\"'`~<>,.?/]).{12,}$",
          "validationMessage": "Password must be at least 12 characters long and include upper, lower, number, and special character."
        },
        "visible": true
      }
    ],
    "steps": [],
    "outputs": {
      "adminPassword": "[basics('adminPassword')]"
    }
  }
}
